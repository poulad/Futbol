{
  "name": "futbol-deploy",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "deploy": "node deploy.js"
  },
  "private": true,
  "devDependencies": {
    "@types/shelljs": "^0.8.0",
    "chalk": "^2.4.1",
    "shelljs": "^0.8.2"
  }
}
